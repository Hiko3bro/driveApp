# Changelog

このファイルには、ユーザーから見える変更を記録します。日々の詳細な作業ログは [docs/PROGRESS.md](./docs/PROGRESS.md) を参照してください。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) を参考にしています。

## [未リリース]

### 追加

- ドライブ条件入力画面を「今日はどんなドライブにする?」という体験に作り替えた。「今日の気分」(景色重視/海沿い/山道/夜ドライブ/のんびり/寄り道多め/短時間/帰宅時間重視)を最大3つまで複数選択でき、「どれくらい走る?」(1〜3時間くらい/半日/時間を指定)、「戻り方」、「何時ごろ戻る?」もチップで選べるようになった。すべて任意で、何も選ばなくても次へ進める
- 「どれくらい走る?」で「時間を指定」を選ぶと、30分〜5時間の候補チップから使える時間を選べるようになった。既存の時間予算計算(使える時間と帰着時刻の短い方を採用する仕組み)をそのまま再利用している
- 選んだ「今日の気分」に応じて、提案される3ルート(景色重視ルート/バランスルート/のんびりルートまたは短時間ルート)の速さ・寄り道の数・タグ・「どんな人向けか」が変わるようになった。既存の3ルート構造・時間予算チェックは維持したまま、モックのルート生成ロジックだけを条件に応じて作り分けるよう改善した
- ルート比較画面・ルート決定確認画面の上部に、選んだ条件が「景色重視・海沿い・2時間くらい」のようにひと目で分かる表示を追加した
- 寄り道スポット探索画面の上部に「おすすめ/ごはん/カフェ/絶景/温泉/アクティビティ」のカテゴリチップを追加した。切り替えると地図上のマーカーとスポットカードが同じカテゴリで絞り込まれ、「おすすめ」では全カテゴリから表示される
- スポットのモックデータをカテゴリ対応させ、5カテゴリそれぞれに複数のスポットを用意した(Google Places API等の外部APIは今回も未使用)
- 寄り道スポット探索画面とルート確認画面のボタン・ラベル文言を、「経由地に追加」→「ここ寄ってみる」、「経由地から外す」→「行くのをやめる」、「追加した経由地」→「寄るところ」のように、ナビ用語から柔らかい言い回しへ変更した(スポットを1件も選ばずに進める挙動は変更していない)
- 日記確認画面から「共有する」を押すと、共有画面(`app/drive-diary-share.tsx`)へ進めるようになった
- 共有画面で、PHOTO(写真を背景にGPSルート線をオーバーレイ)/DATA(走行距離・時間・経由スポット数などの数字を主役にしたカード)/MEMORY(写真・タイトル・メモに小さなGPSルート線を添えたカード)の3種類のテンプレートを切り替えてプレビューできるようになった。写真がない日記でも、安全な背景・プレースホルダーで表示できる
- 共有画像内のGPSルート線は、記録した生の緯度経度をそのまま使わず、共有カード内の相対座標へ正規化したうえで開始・終了地点付近を既定の割合だけ間引いて描画するようになった。地図タイルやマップアプリのスクリーンショットは使用しない
- 共有プレビューの「共有する」を押すと、生成した画像をOS標準の共有シート(Instagram/LINE/AirDrop/保存など、端末にインストールされたアプリに応じた選択肢)へ渡せるようになった。Instagram専用APIへは接続しない
- 記録結果確認画面から「日記を作成」を押すと、日記作成画面(`app/drive-diary-create.tsx`)へ進めるようになった
- 日記作成画面で、タイトル・ひとことメモ・日付を入力できるようになった。走行距離・走行時間・選択したルート・経由したスポットは記録結果からそのまま表示する(手入力での編集は不可)
- 日記作成画面で、端末の写真ライブラリ(expo-image-picker)から写真を最大3枚選べるようになった。写真ライブラリへのアクセスが許可されない場合やExpo Go等で使えない場合も、案内メッセージを表示したうえで写真なしのまま日記作成を続けられる
- 「日記を保存」を押すと日記確認画面(`app/drive-diary-confirm.tsx`)へ進み、保存したタイトル・メモ・日付・写真・走行距離・走行時間・選択したルート・経由したスポットを確認できるようになった。保存した日記はアプリ内状態(React Context)に保持し、将来の共有機能から利用できる形にした
- ルート決定確認画面の完了画面から「ドライブを開始」を押すと、ドライブ記録画面(`app/drive-recording.tsx`)へ進めるようになった
- ドライブ記録画面で、経過時間・走行距離・現在地・記録状態(記録前/記録中/記録終了)を表示しながら走行を記録できるようになった。実GPS(expo-location)が使える場合は5秒ごとに位置を記録し、Expo Go・権限拒否・シミュレーター等で実GPSが使えない場合は選択中ルートの経路に沿って自動的に往復する「デモ走行モード」へフォールバックする
- 「ドライブを終了」を押すと記録結果確認画面(`app/drive-summary.tsx`)へ進み、走行時間・走行距離・記録した座標数・選択したルート・経由したスポットを確認できるようになった。この記録結果はアプリ内状態(React Context)で保持し、次の日記作成機能から利用できる形にした
- ホーム画面から、出発地点の選択(現在地/自宅/地図で指定)、ドライブ条件の入力(使える時間・気分・帰着地点・帰着時刻・寄り道の量)、モックデータによる3ルート(景色重視/寄り道重視/短時間)の提案と地図上での比較まで、一連の操作ができるようになった
- 出発地点として「自宅」を選ぶと、初回は地図上で自宅の場所を登録でき、次回以降は端末内(expo-secure-store)に保存した座標が使われるようになった
- 自宅が登録済みの場合、「この自宅を使う」に加えて「自宅を変更」を選べるようになった。変更時は登録済みの場所を初期位置として地図が開き、登録し直すまでは元の自宅が保持される
- 現在地から出発する場合、位置情報の利用許可を求め、拒否した場合もエラー画面にならず、他の出発地点を選び直せるようになった
- ルート比較画面で「このルートにする」を押すと、選択したルート名・距離・所要時間・特徴を表示する「ルート決定確認」画面(`app/route-summary.tsx`)へ進むようになった(戻る操作でルート比較画面へ戻れる)

### 修正

- 寄り道スポット探索画面で、地図を手動でズーム・パンした後にマーカーやカードを選択すると、地図が全体表示へ戻ってしまいズームが解除される不具合を修正。選択時はユーザーが操作した現在のズーム量を保ったまま、選択したスポットへ軽くセンタリングするだけにした
- 自宅の地図中心取得中・SecureStore保存中は、端末・ジェスチャー・ヘッダーを含む戻る操作で画面を離れないよう修正
- 日付変更線をまたぐルートの地図範囲を円周上の最小経度範囲から計算し、カメラが世界全体へ広がらないよう修正
- 自宅変更と指定場所の確定時に、イベントから保持した座標ではなく確定時点の地図カメラ中心を取得して使用するよう修正。地図準備中・座標取得中・保存中の連打を防ぎ、取得失敗時は再試行を案内する
- 現在地取得・自宅保存の完了前に画面を離れた場合、古い非同期処理が出発地点や画面遷移を後から変更しないよう修正
- 自宅・指定場所・モックルートの座標を厳密に検証し、生成座標の緯度を有効範囲へ収め、経度を正規化するよう修正
- モックルートの往路・寄り道・帰路を含む所要時間が、使える時間と帰着期限から求めた時間予算内に収まるよう修正。期限超過など生成不能時は条件変更を案内する
- 地図のfallbackから特定の実在目印の正確な座標を削除し、丸めたデモ地域へ変更
- Expo Goでの実機確認(1回目)で見つかった不具合を修正・実機で解消を確認済み:
  - ルート比較画面で3つ目のルート(短時間ルート)を選ぶとクラッシュすることがあった地図カメラ制御の不具合を修正
  - 3つの提案ルートを何度も切り替えるとクラッシュしやすかった問題を修正(地図カメラの命令的な制御への一本化、ルート単位でのMarker/Polylineの作り直し)
  - 出発地点選択→ドライブ条件入力→戻る、と操作すると「現在地を取得しています」の表示が終わらなくなっていた不具合を修正
- Expo Goでの実機確認(2回目)で見つかった不具合を修正(実機での再確認は待ち):
  - 出発地点選択画面で「自宅を変更」「指定した場所」の地図を操作するとクラッシュすることがあった問題を修正(地図の初期位置と、パン操作で動いた中心座標を別々に管理するよう分離)
  - 「このルートにする」を押すと過去の画面(条件入力画面や出発地点選択画面)へ戻ってしまうことがあった不具合を修正(ルート決定確認画面への前進のみを行い、途中でDriveFlowContextを初期化しないようにした)
  - 出発地点を「指定した場所」にした場合に、ルート決定確認画面まで進むと出発地点選択画面の地図ピッカーが勝手に再表示されることがあった不具合を修正
  - 「このルートにする」を連続で押しても画面が複数回開かないよう二重タップ防止を追加

### 依存関係

- 地図表示・ルート比較のために `react-native-maps` を追加(Web版は未対応のためプレースホルダー表示)
- 現在地取得のために `expo-location` を追加
- 自宅座標の安全な保存のために `expo-secure-store` を追加
- 日記作成時の写真ライブラリ選択のために `expo-image-picker` を追加
- 共有カードの画像生成のために `react-native-view-shot` を追加
- 生成した画像をOS標準の共有シートへ渡すために `expo-sharing` を追加

### ドキュメント

- Claude Code / Codex / GitHub で安全に共同開発するための基盤ドキュメントを整備(README.md, AGENTS.md, CLAUDE.md, docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/DECISIONS.md, docs/PROGRESS.md)
- `.env.example` を追加(実際の秘密情報は含まない、将来使用予定の環境変数名のみ)
- `.gitignore` に `.env` / `.env.local` を追加
