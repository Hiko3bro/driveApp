# 設計判断記録(Decision Log)

重要な設計判断とその理由を記録します。新しい決定は末尾に追記してください。

## 記録フォーマット

```
### YYYY-MM-DD: タイトル
- 背景:
- 決定:
- 理由:
- 代替案:
```

---

### 2026-08-30: Claude Code / Codex / GitHub 共同開発のためのドキュメント基盤を整備

- 背景: 複数のAIエージェント(Claude Code, Codex)とGitHub上で安全に共同開発するための共通ルール・ドキュメントが存在しなかった
- 決定: README.md / AGENTS.md / CLAUDE.md を整備し、docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/DECISIONS.md, docs/PROGRESS.md, CHANGELOG.md, .env.example を新規作成する
- 理由: mainブランチへの直接変更や秘密情報の誤コミットを防ぎ、進捗・設計判断を一貫した場所に記録することで、複数エージェント・複数セッションをまたいだ協業を可能にするため
- 代替案: すべてを1ファイルに集約する案もあったが、GitHubや進捗確認アプリで表示されるREADME(現在の正しい状態のみを保つ)と、詳細な作業履歴(PROGRESS.md / CHANGELOG.md)とでは求められる粒度・更新頻度が異なるため分離した

### 2026-08-30: 有料APIを現時点で導入しない

- 背景: Google Routes / Google Places, Supabase, AI APIなど有料サービスの利用が将来的に想定される
- 決定: 現時点ではこれらを導入せず、無料の範囲・モックデータ・ローカル状態管理で機能検証を進める
- 理由: コスト発生と外部依存を避けつつ、先にUI/UX・情報設計・体験そのものを固めるため
- 代替案: 早期にAPIを導入し実データで検証する案もあったが、コスト管理とスコープ管理の観点から現時点では見送った

### 2026-08-30: 地図表示はプラットフォーム別実装(ネイティブ/Web)で差し替える

- 背景: `react-native-maps` はWebプラットフォームに対応しておらず、そのままではWeb向けビルド(`expo start --web`)が壊れる
- 決定: `components/map/drive-map-view.tsx`(ネイティブ用、react-native-maps使用)と `components/map/drive-map-view.web.tsx`(Web用、プレースホルダー表示)をMetroのプラットフォーム別拡張子で用意し、呼び出し側は同じpropsで両方に対応させる
- 理由: アプリ本体はモバイル向け(iOS/Android)が主眼だが、`tsc`/`lint`/`expo start --web`によるCI的な検証は引き続きWebでも通したいため。react-native-mapsをWebでそのまま読み込むとバンドルや実行時にエラーになる
- 代替案: Web対応の地図ライブラリ(react-native-web-maps等)を追加する案もあったが、「不要なパッケージを勝手に追加しない」方針と、Webはこのアプリの主要な利用環境ではないことから見送った

### 2026-08-30: 出発地点・ドライブ条件・提案ルートはReact Contextで画面間共有する

- 背景: ホーム→出発地点選択→条件入力→ルート比較、の4画面にまたがって、座標やルートのモックデータ(配列・オブジェクト)を受け渡す必要がある
- 決定: URLクエリパラメータではなく、`contexts/drive-flow-context.tsx` のReact Contextでアプリ内状態として保持する
- 理由: 緯度経度や配列などURLに載せにくい/載せるべきでないデータを扱うため。位置情報は特に慎重に扱うべき情報であり、URLやディープリンクに残したくない
- 代替案: 状態管理ライブラリ(Zustand等)の導入も考えられるが、画面数・状態が小さい現段階ではReact標準のContextで十分と判断した

### 2026-08-30: 出発地点・自宅の場所指定は「地図中央のピン」方式にする

- 背景: 「指定した場所」「自宅の登録」では、地図上でユーザーに座標を選んでもらう必要がある
- 決定: 地図をタップして個別のマーカーを打つ方式ではなく、地図を動かして中央の固定ピンに合わせる方式(Uber/Google Maps等でよく使われるUX)を採用する
- 理由: タップ位置から正確な座標を取るにはネイティブ側のタップ座標変換が必要でやや複雑になるのに対し、中央ピン方式は`onRegionChangeComplete`で得られる地図中心の座標をそのまま使えばよく、実装がシンプルかつ誤タップが起きにくい
- 代替案: 地図タップで個別マーカーを設置する方式も検討したが、今回のスコープでは中央ピン方式で要件(地図上で場所を指定できる)を満たせるため採用しなかった

### 2026-08-30: 地図で確定する座標は確定時のネイティブカメラ中心を正とする

- 背景: `onRegionChangeComplete`で更新したReact stateは、実機でイベントが遅延・省略された場合に、画面上の中央と一致しない可能性がある
- 決定: `pickedCenter`は補助値に限定し、自宅・指定場所の確定時は`react-native-maps`の`getCamera()`から中心を取得して厳密に検証する
- 理由: ユーザーが確定ボタンを押した瞬間のネイティブ地図を、保存・Context更新に使用する単一の正とするため
- 代替案: controlledな`region`で常時同期する案は、ユーザー操作とアプリ側カメラ制御の競合を避けるため採用しない

### 2026-08-30: モックルートも帰路を含む時間予算を厳守する

- 背景: 各区間へ同じ半径を掛ける方式では、寄り道数が増えるほど選択時間を超え、帰着時刻も生成条件に反映されていなかった
- 決定: 使える時間と帰着期限までの残り時間の短い方を有効予算とし、往路・全寄り道・帰路を組み立てた所要時間が予算内になる最大縮尺を探索する
- 理由: UIで選択した時間制約を、表示だけでなく実際のルート生成へ一貫して適用するため
- 代替案: 区間数だけで距離を等分する方式は、ルート形状によって帰路距離が変わり予算超過を保証できないため採用しない

### 2026-08-31: 現在のリリース・実機検証対象をiOSに定める

- 背景: iOSのExpo Go実機でコアルート探索フローを確認できた一方、Android実機を所有しておらず、同等の実機検証を継続的に行える環境がない
- 決定: 現在のリリース目標をiOS TestFlightとし、iOSを正式な実機検証対象とする。Androidは将来対応予定とし、React Native / Expoによる対応可能な共通構造は維持するが、実機検証を行うまでは動作保証の対象外とする
- 理由: 検証可能なプラットフォームへ品質基準を明確に絞り、未検証のAndroidを対応済みと誤認させずにiOSリリース準備を進めるため
- 代替案: iOSとAndroidの同時リリースまで保留する案もあるが、Androidの検証環境がない現状ではリリース時期が不明確になるため採用しない

### 2026-08-31: ドライブ記録画面は地図を持たない構成にする

- 背景: `route-plan`でMapView(react-native-maps)をマウントするとiOS + Expo Goでネイティブクラッシュする既知の問題があり(2026-08-31(2)参照)、原因の切り分けが完了していない
- 決定: 新設のドライブ記録画面(`app/drive-recording.tsx`)ではMapViewを一切マウントせず、現在地は緯度経度のテキスト表示にとどめる
- 理由: 既知のクラッシュ条件が完全には特定できていない状況で、record中に継続的に位置更新が入る新しい地図表示を追加すると、同種のクラッシュを再現・悪化させるリスクが高いため。記録機能自体の一連の体験(開始→記録→終了→結果確認)を安全に検証できることを優先した
- 代替案: `route-plan`と同様にプレースホルダー画像上に地図風の表示を作る案も検討したが、実装コストの割に得られる価値が小さく、今回のスコープ(記録の一連の体験)には不要と判断し見送った。地図表示(記録ルートの可視化)はDevelopment Buildでの既知の問題再現状況を確認した後に改めて検討する

### 2026-08-31: 実GPSが使えない場合は選択中ルートの経路に沿ったデモ走行モードへフォールバックする

- 背景: Expo Go・位置情報許可拒否・シミュレーター等、実GPSの現在地が取得できない環境でも、ドライブ記録の一連の体験(開始→記録→終了→結果確認)を確認できる必要がある
- 決定: 記録開始時に現在地取得(`requestCurrentLocation`)を試み、失敗した場合は選択中ルートの`route.path`(モックの経路座標)に沿って一定周期で前後に往復する座標を生成し、実GPSと同じ記録間隔・同じ距離計算ロジックで記録する
- 理由: 実在しないモック経路を使うことで、位置情報を偽装しても個人の実際の位置情報が記録・表示されることはない。また既存のルート表示に使っている座標をそのまま再利用でき、新たなモックデータを増やさずに済む
- 代替案: 固定の1点を返す、あるいはランダムウォークで座標を生成する案も検討したが、選択したルートに沿って動くほうが実際の記録体験に近く、確認しやすいため採用しなかった

### 2026-08-31: ドライブ記録結果は現段階ではReact Context(アプリ内状態)のみに保持する

- 背景: 記録終了後の走行時間・走行距離・記録した座標配列・選択したルート・経由したスポットを、次フェーズの日記作成機能から利用できる形で保持する必要があるが、本格的なクラウド保存は今回のスコープ外
- 決定: `contexts/drive-flow-context.tsx`に`driveRecord`(記録結果のスナップショット1件)を追加し、アプリ内状態としてのみ保持する。端末への永続化(AsyncStorage等)やクラウド保存(Supabase等)は今回導入しない
- 理由: 既存のdeparture/conditions/routes/spotsと同じ、画面間で共有するだけのセッション内状態として扱うのが最小の変更で済むため。将来、個人のドライブ記録をローカル優先で永続化し、公開・同期対象のみクラウドへ分離する方針(要件として提示済み)に合わせて、`driveRecord`は他の状態から独立したフィールドとして分離してあり、後からローカル永続化層・クラウド同期層を追加しやすい構造にしている
- 代替案: 記録開始時点で`AsyncStorage`等へ都度書き込む案も検討したが、「不要なパッケージを勝手に追加しない」方針と、今回のスコープが「一連の体験をローカル状態で確認できること」である点から見送った

### 2026-08-31: 記録データ(生のGPS座標配列)と表示・共有用データを結合しない

- 背景: 将来的にNike Run Clubのような「走行したGPSルート線を写真に重ねて共有する」機能を想定しており、共有画像では開始地点・終了地点付近を隠すプライバシーマスクを別途適用する予定がある
- 決定: `types/drive-recording.ts`の`RecordedTrackPoint`(座標・記録時刻のみを持つ)を記録データの唯一の形式とし、地図表示や共有画像生成のための座標変換・マスク処理・間引き処理などは行わない。デモ走行モードの座標生成(`services/location/drive-demo-simulator.ts`)も、表示用の補間ではなく記録データそのものとして同じ形式で返す
- 理由: 記録データに表示都合の加工(間引き、マスク、座標補正など)を先に加えてしまうと、将来のプライバシーマスクや共有画像生成のときに「元の記録データ」が失われてしまう。生データと表示用データの変換を後から追加できるよう、今回は生データの保持のみに専念した
- 代替案: 記録時点で表示用に座標を間引く案も検討したが、後工程(共有機能)の要件が未確定な現時点では、情報量を落とさない生データのまま保持するほうが安全と判断した

### 2026-08-31: 日記の写真選択に`expo-image-picker`を追加する

- 背景: 日記に端末の写真ライブラリから1〜3枚の写真を添付する要件があるが、React Native標準にはこの機能がなく、既存の依存関係にも写真ライブラリへアクセスできるものがない
- 決定: Expo公式の`expo-image-picker`を追加する。導入前にユーザーへ確認し、合意のうえで`npx expo install expo-image-picker`を実行した
- 理由: CLAUDE.md/AGENTS.mdの「不要なパッケージを勝手に追加しない」方針により、新規パッケージの追加は事前合意を必須とした。Expo公式パッケージで許可ダイアログ・複数選択(`selectionLimit`)を標準サポートしており、既存の`expo-location`等と同じ「許可ダイアログ→許可/拒否のハンドリング」パターンを踏襲できる
- 代替案: 写真選択を今回のスコープから外し、日記データ構造にphotos欄だけ用意して後回しにする案も提示したが、ユーザーが`expo-image-picker`の追加を選択した

### 2026-08-31: 日記データ(`DriveDiaryEntry`)はドライブ記録と同じくReact Contextのみに保持し、プレーンな構造で設計する

- 背景: 日記の走行距離・走行時間・選択したルート・経由したスポット・記録したGPS座標配列(track)は、既に`driveRecord`(記録結果)として保持済みのデータであり、日記作成時に新たに入力し直す必要がない。また将来的にAsyncStorage等へ永続化する予定がある
- 決定: `contexts/drive-flow-context.tsx`に`diaryEntries: DriveDiaryEntry[]`と`latestDiaryEntryId`を追加し、日記作成時は`driveRecord`から走行距離・走行時間・route・spots・trackをそのままコピーして1件の`DriveDiaryEntry`を組み立てる。`DriveDiaryEntry`はクラスやDate型を持たない、JSON化可能なプレーンオブジェクトとして設計した
- 理由: プレーンなオブジェクト配列にしておくことで、将来`AsyncStorage`(または他の永続化手段)へ`JSON.stringify`でそのまま書き出せる。`driveRecord`のスナップショットをコピーすることで、日記保存後に別のドライブを記録してもその日記の内容が変化しない(記録終了時点の状態で固定される)
- 代替案: `driveRecord`への参照(id等)だけを日記に持たせる案も検討したが、将来的に「1つのドライブから複数の日記は作らない」制約が崩れたり、`driveRecord`が上書きされた場合に過去の日記が壊れるリスクがあるため、値をコピーする方式を採用した

### 2026-08-31: 走行距離・走行時間・選択したルート・経由したスポットは日記作成画面で編集できないようにする

- 背景: 要件の「日記には以下を表示・編集できる: タイトル/一言・メモ/日付/走行距離/走行時間/選択したルート/経由スポット」という記述は、フィールドの列挙であり、走行距離等を自由入力させる意図ではないと解釈した
- 決定: タイトル・メモ・日付のみをテキスト入力とし、走行距離・走行時間・選択したルート・経由したスポットは記録結果(`driveRecord`)からの読み取り専用表示にする
- 理由: 走行距離・走行時間はGPS記録(または記録時のデモ走行)から得られた事実であり、自由に書き換えられると記録データとしての信頼性が失われる。選択したルート・経由したスポットも記録結果の一部として扱うのが自然
- 代替案: すべてのフィールドを自由入力にする案もあったが、記録データの整合性を優先し見送った。将来的に「記録を後から手動修正したい」という要件が明確になった場合は、別途上書き用のUIを検討する

### 2026-09-01: 共有画像の生成・共有シート起動に`react-native-view-shot`・`expo-sharing`を追加する

- 背景: PHOTO/DATA/MEMORYの共有カードを実際の画像として生成し、OSの共有シート(Instagram/LINE/AirDrop/保存等)へ渡す要件があるが、React Native/Expo標準にはView(画面の一部)を画像化する手段がなく、既存の依存関係にも該当するものがない
- 決定: 画像化には`react-native-view-shot`、共有シートの起動には`expo-sharing`を追加する。導入前にユーザーへ確認し、両パッケージの追加・GPSルート線の描画方法(react-native-svgを追加せずViewの組み合わせで描く)について合意を得たうえで`npx expo install`を実行した
- 理由: CLAUDE.md/AGENTS.mdの「不要なパッケージを勝手に追加しない」方針、および今回の要件で明示された「画像生成に追加パッケージが必要な場合は先に確認する」に従った。`react-native-view-shot`はExpo Goでも動作実績のある定番ライブラリで、Viewツリーをそのままキャプチャできるため、共有カードのレイアウトをReact Nativeのコンポーネントとして素直に実装できる。`expo-sharing`はExpo公式のファイル共有パッケージで、React Native標準の`Share` APIよりファイル(画像)共有の挙動が安定しており、将来Androidへ対応する際も同じAPIを使える
- 代替案: 画像生成自体を見送りテキストのみ共有する案、共有はReact Native標準の`Share` API(iOSの`url`のみ)で済ませる案、GPSルート線を`react-native-svg`で描く案をそれぞれユーザーに提示したが、画像生成・expo-sharing・Viewベースの線描画(パッケージ追加を1つ減らす)の組み合わせが選ばれた

### 2026-09-01: 共有用GPSルート線は生のtrackを変換した相対座標(SharePoint)として保持し、地図タイルは使わない

- 背景: `DriveDiaryEntry.track`は生の緯度経度配列であり、これをそのまま共有画像の座標系(カード内のピクセル位置)として使うことはできない。またApple Maps/Google Mapsのスクリーンショットを背景に使うことは要件で禁止されている
- 決定: `services/sharing/share-route.ts`の`projectTrackToSharePoints()`が、`track`の緯度経度の分布(最小・最大値の範囲)から0〜1の相対座標(`SharePoint`)を計算する。緯度方向の1度あたり距離を基準に、`cos(平均緯度)`で経度方向のスケールを補正し、ルート線の形が実際の移動比率に近くなるようにした。`track`自体は書き換えず、常に新しい配列を返す
- 理由: 「地図タイルやマップアプリのスクリーンショットを背景に使わない」という要件を満たしつつ、Nike Run Clubのような「ルート線だけ」のグラフィックを作るには、緯度経度をカード内のローカルな相対座標へ変換する以外に方法がない。生のtrackとは別の`SharePoint[]`という型にすることで、共有表示用データと記録データを明確に分離した
- 代替案: 正距円筒図法(緯度経度をそのまま比例縮小)も検討したが、経度方向の歪みが大きくなる高緯度地域で線の形が不自然になるため、簡易的な緯度補正を addした現在の方式を採用した。より正確な地図投影法(メルカトル図法等)の実装は、今回の「シンプルなルート線」という要件を超えるため見送った

### 2026-09-01: 共有用ルート線の開始・終了地点付近を、既定で一部間引く小さなユーティリティを用意する

- 背景: 「将来的に開始地点・終了地点付近を非表示にするプライバシーマスクを追加する」ことを見据え、生trackと共有用trackを分離してほしいという要件があった。あわせて「可能であれば、開始・終了地点の一定割合を除外できる小さなユーティリティを作ってほしい。ただし今回の目的を超える大規模実装にはしない」という指示があった
- 決定: `services/sharing/share-route.ts`に`maskRouteEndpoints(points, ratio)`という汎用的な小さい関数を実装し、`projectTrackToSharePoints()`から既定の割合(8%、最大40%までにクランプ)で呼び出す形にした。自宅や出発地点そのものをぼかす本格的なマスク(道路網を考慮した除外範囲の計算など)は実装していない
- 理由: 配列の前後を一定割合だけ取り除くだけの単純な実装であれば「大規模実装」にならず、かつ実際に共有画像上で開始・終了地点付近の座標が(間引かれることで)完全な位置には出ない状態を今の時点で作れる。将来、本格的なプライバシーマスク(道路上の距離基準での除外、UI上でON/OFFを選べる設定など)を追加する際も、この関数のratio引数を差し替えるか、上位のロジックを追加するだけで拡張できる
- 代替案: マスク機能を実装せず型定義だけ用意する案も検討したが、「可能であれば作ってほしい」という要件に対して実際に動くユーティリティを用意する方が要件を満たせると判断した

### 2026-09-01: スポットのカテゴリ切り替えは既存の`SpotCategory`型の値を差し替えて実現する(新しいフィールドは追加しない)

- 背景: スポット探索画面にカテゴリタブ(おすすめ/ごはん/カフェ/絶景/温泉/アクティビティ)を追加する必要があるが、既存の`Spot.category: SpotCategory`(絶景/カフェ/グルメ/休憩/穴場)は表示バッジにも使われている
- 決定: バッジ表示用の`category`とは別に新しいフィールド(`browseCategory`等)を追加するのではなく、`SpotCategory`の許容値そのものを新しい5カテゴリ(ごはん/カフェ/絶景/温泉/アクティビティ)へ差し替えた。「グルメ」は「ごはん」に、「休憩」「穴場」は文脈に近い「温泉」「アクティビティ」へ再割り当てし、モックデータのテンプレートも作り直した。「おすすめ」は`Spot`が持つ値ではなく、画面側の絞り込み条件だけを表す`SpotBrowseFilter`型(`'おすすめ' | SpotCategory`)として別に定義した
- 理由: カードのバッジ表示とカテゴリ絞り込みが別々の語彙を持つと、同じカードに矛盾する2つのカテゴリ表記が並んでしまい(例: バッジは「グルメ」なのにタブは「ごはん」)、かえって分かりにくくなる。`category`の意味を「このスポットが属するカテゴリ」という1つの概念に統一した方が、実装もシンプルで理解しやすい。`SpotCategory`という型名・`category`というフィールド名自体は変更していないため、大規模なリネームにもあたらない
- 代替案: `category`(バッジ表示用)と`browseCategory`(絞り込み用)を別々に持たせる案も検討したが、二重管理になり値の矛盾が起きやすいため見送った

### 2026-09-01: 「おすすめ」タブは絞り込みなしで全カテゴリのスポットをそのまま表示する

- 背景: 要件に「『おすすめ』では全カテゴリからおすすめスポットを表示してください」とあり、特定のスポットを「おすすめ」として選び出す仕組みが必要か検討が必要だった
- 決定: `Spot`に「おすすめフラグ」のような新しい属性は追加せず、「おすすめ」タブでは`selectedCategory === 'おすすめ'`のときにフィルタ処理そのものをスキップし、そのルートで見つかった全スポットをそのまま表示する
- 理由: 現状のモックスポットは(将来はGoogle Places API接続後の)厳選候補という位置づけで、件数も1ルートあたり数件程度のプロトタイプ規模のため、「全部がおすすめ」という扱いで要件(全カテゴリから表示)を満たせる。新しい属性を追加すると、モックデータ・型・UIのすべてに手を入れる必要があり、今回のスコープに対して過剰になる
- 代替案: 各カテゴリから1〜2件を手動で「おすすめ」指定する案も検討したが、スポット件数が少ない現段階では効果が薄く、大規模実装を避ける方針にも反するため見送った

### 2026-09-01: 「経由地」等の言い換えは、寄り道スポット探索画面とその直前直後の画面に限定する

- 背景: 要件は「ユーザー向けUIでは『経由地』『経由スポット』などの堅いナビ用語をできるだけ避けてください」というもので、対象範囲の明確な指定はなかった。一方で「大規模なリネームはしないでください」という制約もあった
- 決定: 実際に文言変更したのは`app/spot-discovery.tsx`(スポット探索画面本体)、`app/route-plan.tsx`(スポットを選んだ結果を確認する画面)、`app/route-summary.tsx`(スポット探索へ入る前の案内文)の3画面のみとした。`app/drive-recording.tsx`・`app/drive-summary.tsx`・`app/drive-diary-create.tsx`・`app/drive-diary-confirm.tsx`(ドライブ記録・日記のフェーズで新設した画面)は今回変更していない
- 理由: 今回のタスクは「既存のスポット探索機能を改善する」ことが目的であり、対象は「ルート決定→スポット探索→ルート確認」の一連の体験に限定するのが妥当と判断した。ドライブ記録・日記の画面群は別フェーズ(`feature/drive-recording`・`feature/drive-diary`)で作られたものであり、今回むやみに手を広げると「大規模なリネームはしない」という制約に反するおそれがあり、無関係な差分でブランチ統合時のコンフリクトリスクも増やすため見送った
- 代替案: アプリ全体の「経由地」表記を一括で置き換える案も検討したが、変更範囲がスポット探索の体験を大きく超えるため採用しなかった。将来ドライブ記録・日記画面の文言を見直す場合は、別タスクとして扱う

### 2026-09-01: スポット探索画面のカメラ制御を「全体表示」と「個別選択」の2責務に分離する

- 背景: 実機確認で、地図を手動でズーム・パンした後にマーカーやカードを選択すると、地図が全体表示へ戻ってズームが解除される不具合が見つかった。原因は、マーカー/カード選択時に呼んでいた`computeRegionForPath([...route.path, spot.coordinates])`が、選択したスポット1点だけでなくルート全体の座標列も範囲に含めて計算する「全体を収める」関数であり、実質的に初回表示・カテゴリ切り替え時と同じ「fit全体」処理を毎回再実行していたことだった
- 決定: 地図に今表示されているregion(ズーム量を含む)を`onRegionChangeComplete`経由で`currentRegionRef`に保持し続けるようにし、マーカー/カード選択時(`handleSelectSpot`)はこの保持しているregionの`latitudeDelta`/`longitudeDelta`(現在のズーム量)だけを引き継いで、中心座標だけを選択スポットへ移すregionを作り、`animateToRegion`する形にした。ルート・カテゴリ全体を収める`computeRegionForPath`の呼び出しは、初回表示・カテゴリ切り替え時の`useEffect`(ルート+カテゴリの組み合わせごとに一度だけ発火)だけに残した
- 理由: 「全体を収める」と「個別スポットへ寄せる」は本質的に別の責務であり、同じ`computeRegionForPath`を両方に使い回していたことが不具合の原因だった。役割を分けることで、全体表示はルート・カテゴリが変わったときだけ、個別選択時のカメラ移動はユーザーの現在のズームを尊重したセンタリングだけ、と挙動が明確になる。`currentRegionRef`の値がまだない場合(ズーム量を一度も観測できていない極端なタイミング)のみ、安全側のフォールバックとして`computeRegionForPath`による全体表示を使う
- 代替案: `DriveMapView`に「現在のズームを保ったまま指定座標へ移動する」ための新しいメソッドを追加する案も検討したが、`onRegionChangeComplete`は既にコンポーネントが対応済みのpropであり、呼び出し側(`spot-discovery.tsx`)だけの変更で実現できるため、コンポーネント自体は変更しない今回の方式を採用した

### 2026-09-01: 「今日の気分」は`Mood`型・`MOOD_LABELS`をそのまま流用し、値だけを新しい8択へ差し替える

- 背景: ドライブ条件入力画面に「景色重視/海沿い/山道/夜ドライブ/のんびり/寄り道多め/短時間/帰宅時間重視」を選べる「今日の気分」を追加する必要があるが、既存の`DriveConditions.mood: Mood`(view/sea/mountain/cafe/nightview/hidden/omakaseの7値)がすでにほぼ同じ役割(ルートの雰囲気を選ぶ)を担っていた
- 決定: `Mood`という型名は変えず、値だけを新しい8値へ差し替えた。フィールド名は`mood`(単一選択)から`moods`(複数選択、配列)へ変更した。この変更は`types/drive.ts`・`app/conditions.tsx`・`services/route/mock-route-provider.ts`の3ファイルに閉じている(他のファイルは`DriveConditions`を丸ごと受け渡すだけで`.mood`を直接参照していないことを確認済み)
- 理由: 新しい8択は、既存の`Mood`(気分)が担っていた役割をそのまま置き換えるものであり、別の新しい型を並行して増やすと`mood`と新フィールドの二重管理になり複雑化する。`mood`→`moods`の複数形化は、単一選択から複数選択への変更を素直に表す最小限のリネームであり、「内部の型名・変数名まで大規模に変更する必要はありません」という指示にも反しない範囲だと判断した
- 代替案: 型名も`DrivePurpose`のように刷新する案もあったが、影響範囲を広げるだけで実利が薄いため見送った。既存の`mood`(単一選択)のまま、新8択のうち1つだけ選べる仕様にする案も検討したが、要件5の例「景色重視・2時間・海沿い」が2つの気分を同時に選んだ状態を示唆しているため、複数選択(`moods: Mood[]`、最大`MAX_SELECTED_MOODS`件)を採用した

### 2026-09-01: 「寄り道の量」は専用チップをやめ、「今日の気分」の選択から自動的に決める

- 背景: 既存の`DetourLevel`(少なめ/普通/多め)は`app/conditions.tsx`に専用のチップ行があったが、新しい「今日の気分」に「寄り道多め」「短時間」という、寄り道量と直接関係する選択肢が含まれることになった
- 決定: 「寄り道」の専用UIセクションを削除し、送信時に`moods`から`detourLevel`を導出する小さな関数(`deriveDetourLevel`)を`app/conditions.tsx`に追加した(`'detourRich'`を選べば`'many'`、`'short'`を選べば`'few'`、それ以外は`'normal'`)。`DetourLevel`型・`services/route/mock-route-provider.ts`の`DETOUR_LEVEL_BUDGET_USAGE`(既存の時間予算配分ロジック)はそのまま再利用し、値の決め方だけを変えた
- 理由: 同じ「寄り道をどれだけしたいか」を、独立したチップと「今日の気分」チップの両方で聞くと、選択が矛盾したときにどちらを優先するか分かりにくくなる。「今日の気分」だけで完結させることで、要件7の「フォームっぽくしすぎない」UIにも近づく
- 代替案: 「寄り道」チップを残し「今日の気分」の該当項目と同期させる案も検討したが、UIが二重になり複雑になるため見送った

### 2026-09-01: 「時間を指定」は自由入力ではなく、分刻みの候補チップにする

> **追記(同日・実機確認前)**: 実際の利用では細かい時間を指定したいケースが多いとのフィードバックを受け、この決定は下記「『自分で入力』は時・分の数字入力にし、候補チップ方式から変更する」により置き換えられた。決定の経緯を残すため、このエントリ自体は削除せずそのまま残す。

- 背景: 要件2は「時間を指定」という選択肢を求めているが、要件3で「複雑な日時計算や外部API連携は不要」「既存の時間予算へ変換できる範囲で対応」と明記されていた
- 決定: `AvailableTime`に`'custom'`を追加し、`DriveConditions.customAvailableMinutes?: number`を新設。UIでは自由入力のテキストボックスや時刻ピッカーを使わず、30分〜5時間の候補チップ(`CUSTOM_MINUTE_OPTIONS`)から選ぶ方式にした。`services/route/time-budget.ts`の`resolveEffectiveTimeBudget`は、既存の4つの固定値(1h/2h/3h/half-day)の分岐はそのまま残し、`'custom'`のときだけ`customAvailableMinutes`(未設定時は2時間相当へフォールバック)を使う分岐を追加しただけで、既存ロジックは壊していない
- 理由: 自由入力のテキストボックスやネイティブの時刻ピッカーを追加すると、入力値の検証・キーボード制御・新しいUIパターンが必要になり、「複雑な日時計算や外部API連携は不要」というプロトタイプの方針、および要件6の「チップ・カード・ボタン等でiPhoneで押しやすく」という指示に反する。候補チップ方式なら、既存の他のチップUIと同じ実装パターンで完結する
- 代替案: `<Stepper>`のような数値の増減UIも検討したが、新しいUIコンポーネントが必要になり実装コストが増えるため、既存のチップと同じ見た目・操作感で済む候補チップ方式を採用した

### 2026-09-01: 3ルート案は固定の3つの役割(景色重視/バランス/3案目)を保ちつつ、中身を条件で作り分ける

- 背景: 要件4は「既存のルート比較3案の構造は維持」しつつ、「景色重視ルート/バランスルート/のんびりルートなど、ユーザーが『違いが分かる』3案になるようモック生成ロジックを改善」することを求めていた
- 決定: `services/route/mock-route-provider.ts`に`buildArchetypes(conditions)`を新設し、3つの役割(景色重視/バランス/3案目)は常に固定のまま、各役割の名前・説明・タグ・「どんな人向けか」・平均速度・経由地の数・時間予算の使用割合を、選んだ`moods`・`detourLevel`から都度組み立てるようにした。3案目だけは「短時間」を選んでいるかどうかで「短時間ルート」と「のんびりルート」を切り替える。既存の二分探索によるルート距離のフィッティングロジック(`buildRoute`内の縮尺探索)はそのまま再利用した
- 理由: 3つのルートを常に同じ3役割で返すことで、既存の`RouteOption[3]`という構造(ルート比較画面のカード3枚・横スワイプ等)を一切変更せずに済む。中身(速度・経由地数・文言)だけを条件に応じて変えることで、「違いが分かる3案」という要件を、既存の設計を壊さずに満たせる
- 代替案: 条件の組み合わせごとに全く異なる名前・IDのルートを動的生成する案も検討したが、ルート比較・ルート決定確認・ドライブ記録・日記など複数画面がルートの構造(3件・tags・description等)に依存しているため、既存の型・件数を保つ今回の方式のほうが安全と判断した

### 2026-09-01: `RouteOption`に`audience`(どんな人向けか)フィールドを追加する

- 背景: 要件4は、ルートカードに「ルート名/所要時間/距離/特徴/どんな人向けか」を表示することを求めていたが、既存の`RouteOption`型には「どんな人向けか」に相当するフィールドがなかった
- 決定: `types/route.ts`の`RouteOption`に`audience: string`を追加し、`services/route/mock-route-provider.ts`の各ルート案で具体的な文言(例: 「景色を眺めながらゆったり走りたい人向け」)を設定した。`RouteOption`を実際に組み立てているのは`mock-route-provider.ts`だけ(`GoogleRoutesProvider`は未実装スタブで例外を投げるのみ)であることを確認したうえで、追加フィールドを必須にした
- 理由: 表示専用の短い説明文であり、既存のtags/description/highlightと同じ「表示用の付加情報」という性質のため、同じ`RouteOption`型に素直に追加するのが最小の変更で済む。オプショナルにせず必須にしたのは、モックプロバイダーが唯一の実装である現段階では、常に値が入っている前提のほうが呼び出し側(`route-compare.tsx`・`route-summary.tsx`)でnullチェックが不要になり、コードがシンプルになるため
- 代替案: `tags`の中に含める案も検討したが、「どんな人向けか」は短い一文であり、タグ(単語)とは表示のされ方が異なるため、別フィールドとして分離した

### 2026-09-01: 「自分で入力」は時・分の数字入力にし、候補チップ方式から変更する

> **追記(同日・実機確認前)**: 「iPhoneではホイール型のTimePickerを使いたい」という追加フィードバックを受け、一時的に下記「`@react-native-community/datetimepicker`を追加し、ホイール型ピッカーへ置き換える」へ変更したが、実機確認でTimePicker方式がうまく動作しなかったため、この数字入力欄方式へ差し戻した(最終的にこのエントリの内容が現在の実装)。決定の経緯を残すため、両エントリとも削除せずそのまま残す。

- 背景: 実機確認前のフィードバックで、実際の利用では細かい時間(例: 2時間30分)や任意の帰着時刻(例: 18:30)を指定したいケースが多く、固定の候補チップ(30分刻みの時間・17:00〜21:00の1時間刻みの帰着時刻)だけでは自由度が不足していると分かった。ネイティブのTimePicker(`@react-native-community/datetimepicker`等)を使う案も検討したが、新規パッケージの追加になるため、実装前にユーザーへ確認した
- 決定: 新規パッケージは追加せず、既存の`TextInput`(日記作成画面等で既に使用)を使った「時間」「分」(使える時間の場合)・「時」「分」(帰着時刻の場合)の2つの数字入力欄に置き換えた。「どれくらい走る?」の「時間を指定」チップは「自分で入力」に改称。「何時ごろ戻る?」は「指定なし」/「時刻を指定」の2択チップにし、「時刻を指定」を選んだときだけ時・分の入力欄を表示するようにした。`returnDeadline`の保存形式(`"HH:mm"`)・`customAvailableMinutes`(分単位)という既存のデータ形状は変更していない
- 入力値の安全な取り扱い: 入力中は数字以外の文字を除去し2桁までに制限するだけに留め(`sanitizeDigits`)、実際に使う値は`clampInt(text, min, max)`で必ず範囲内の整数へ丸める。「時間」は0〜12時間、「分」は0〜59分、「使える時間」の合計は10分(既存の`MIN_ROUTE_BUDGET_MINUTES`相当の下限)〜12時間(720分)に収め、「時」「分」の帰着時刻もそれぞれ0〜23・0〜59へ収める。入力欄からフォーカスが外れた時点(`onBlur`)で表示も丸めた値へ揃え、送信時にも同じ`clampInt`で再度丸めるため、空欄・0・負数相当の入力・桁あふれのいずれでも例外やNaNは発生しない
- 理由: 「複雑な日時計算や外部API連携は不要」「大規模リファクタはしない」というプロトタイプの方針を保ちながら、既存コンポーネントの範囲で「時・分を入力または選択できる」という要件を満たせる。数字入力欄2つという構成は、既存のチップUIとも視覚的になじみ、iPhoneの数字キーボード(`keyboardType="number-pad"`)でも操作しやすい
- 代替案: `@react-native-community/datetimepicker`の追加も提示したが、ユーザーが「既存コンポーネントのみで実装」を選択したため、新規パッケージなしの数字入力欄方式を採用した。将来ネイティブのホイール型ピッカーによる体験向上が必要になった場合は、改めてパッケージ追加を検討する

### 2026-09-01: 条件要約の帰着時刻表記を「◯◯まで」に短縮する

- 背景: 要件で「2時間30分・18:30まで」のような自然な表示が例示されていたが、既存の`summarizeDriveConditions()`は「18:30までに戻る」という、やや長い文言を生成していた
- 決定: `types/drive.ts`の`summarizeDriveConditions()`内の文言を「`${returnDeadline}までに戻る`」から「`${returnDeadline}まで`」に短縮した
- 理由: 「今日の気分: 景色重視・2時間30分・18:30まで」のように、他の要素(気分・時間)と並べたときに自然に読める短い表記の方が、要件の例示に近く、ルート比較・ルート決定確認画面の限られた表示幅にも収まりやすい
- 代替案: 元の「までに戻る」を維持する案もあったが、要件の例示文言により近づけることを優先した

### 2026-09-01: `@react-native-community/datetimepicker`を追加し、ホイール型ピッカーへ置き換える

> **追記(同日・実機確認前)**: 実機確認でTimePicker方式がうまく動作しなかったため、この決定は取りやめ、直前の「時・分の数字入力欄」方式(1つ上のエントリ)へ戻した。`@react-native-community/datetimepicker`は`npm uninstall`で削除し、`app.json`の`plugins`からも該当エントリを削除して、導入前の状態に戻している。うまく動作しなかった具体的な症状の切り分けはこの時点では行っておらず、原因の記録は残していない。決定の経緯を残すため、このエントリ自体は削除せずそのまま残す。

- 背景: 数字入力欄(`TextInput`)方式を一度実装したが、「iPhoneではホイール型のTimePickerを使い、上下スクロールで時刻を選べるようにしてほしい」という追加フィードバックを受けた。以前の選択肢提示(既存コンポーネントのみ/`@react-native-community/datetimepicker`追加)に対して、今回はパッケージ追加を選ぶ回答があった
- 決定: `npx expo install @react-native-community/datetimepicker`で追加した(バージョン8.4.4は`node_modules/expo/bundledNativeModules.json`に記載されている、Expo Go SDK 54が標準搭載しているバージョンと一致することを確認済み。カスタムDevelopment Buildなしでも動作する)。「何時ごろ戻る?」は`mode="time"` `display="spinner"` `is24Hour`で、iOS・Android共通のホイール型ピッカーを常時使う(spinnerは両OSで対応、時刻を扱う`mode="time"`もAndroidNativeProps/IOSNativePropsの両方でサポートされているため)。「どれくらい走る?」の「自分で入力」は、日時ではなく「所要時間」を扱うため`mode="time"`をそのまま流用せず、iOSのみが対応する`mode="countdown"`(Appleの「タイマー」アプリと同じ、時間・分だけのホイール、AM/PM表示なし)を使い、Androidでは`countdown`が未対応のため前回実装した時・分の数字入力欄(`TextInput`)をそのまま残してフォールバックにした(`Platform.OS`で分岐)
- データ変換: `mode="countdown"`のvalue/onChangeはDateの`getHours()`/`getMinutes()`を「経過した時間・分」として扱う一般的な実装パターンを採用し、`minutesToDurationDate()`/`durationDateToMinutes()`という変換関数を追加した。ピッカーの値をそのまま`customAvailableMinutes`にせず、既存の`clampMinutes()`(10分〜720分)を必ず通してから使うことで、ピッカー自体が広い範囲(0〜23時間59分)を選べても、アプリの実用範囲を超えないようにしている。帰着時刻も同様に、ピッカーが返すDateの`getHours()`/`getMinutes()`から`"HH:mm"`文字列を組み立てるだけで、`returnDeadline`の保存形式・`time-budget.ts`のロジックは一切変更していない
- 理由: 実際にiPhoneで指を上下にスクロールして時刻・時間を選ぶ体験は、数字入力欄への手入力よりも直感的で誤入力もしにくい。`@react-native-community/datetimepicker`はExpo Go標準搭載のバンドルモジュールであり、Expo Goのままこの体験を確認できるため、Development Buildへの移行を伴わずに導入できると判断した
- 代替案: 「自分で入力」もAndroidと同じ数字入力欄に統一し、iOSでもホイール型を使わない案もあったが、要件で「iPhoneではホイール型を優先」と明示されていたため、iOS/Android(および将来Web)でUIが異なることを許容し、`Platform.OS`分岐を採用した

### 2026-09-01: 帰着時刻(`"HH:mm"`)を「次に訪れるその時刻」として解釈し、日付をまたぐ判定に対応する

- 背景: 実機確認で、「何時ごろ戻る?」に未来の時刻(特に日付をまたぐ時刻、例: 現在23:10に対して00:30)を指定しても「帰着時刻はすでに過ぎています」という警告が出てしまう不具合が見つかった。原因は`services/route/time-budget.ts`の`minutesUntilDeadline`が、指定したHH:mmを常に「今日の日付」に当てはめて現在時刻と比較しており、日付をまたぐケースを一切考慮していなかったこと。加えて、この誤った判定結果を条件入力画面(`app/conditions.tsx`)側でもライブ表示用に再実装しており、同じ欠陥を持つ判定が2箇所に重複していた
- 決定: `time-budget.ts`に`resolveNextDeadline(deadline, now)`を新設し、「今日の日付にHH:mmを当てはめた日時が現在日時以下なら、翌日の同時刻とみなす」というロールオーバー処理を追加した。既存の`minutesUntilDeadline`はこの関数を使うように書き換え、`resolveEffectiveTimeBudget`(実際のルート生成チェック)はそのまま`minutesUntilDeadline`を呼ぶだけで対応できた。この結果、ロールオーバー後は帰着時刻までの残り分数が常に0より大きくなるため、「帰着時刻はすでに過ぎています」というメッセージ自体が発生しなくなり、`resolveEffectiveTimeBudget`からその分岐を削除した。条件入力画面側で重複して持っていたライブ判定(`isReturnDeadlineInPast`)も、同じ理由で削除し、判定ロジックを`time-budget.ts`の1箇所に統一した
- 理由: 「HH:mmだけでは今日の過去時刻か翌日かを判断できない」という要件どおり、常に「次に訪れるその時刻」という意味で解釈すれば、当日・翌日どちらのケースも同じロジックで正しく扱え、「すでに過ぎている」という誤判定自体が構造的に起こらなくなる。判定ロジックを1箇所に統一したことで、今後同様の不具合が別画面で再発するリスクも減らせる

### 2026-09-03: 条件入力を1画面フォームから多段階ウィザードへ再設計する

- 背景: 「AIと一緒にドライブを考えていく」体験にするため、1画面フォーム(`app/conditions.tsx`)だった条件入力を、出発地点→今日の気分→どれくらい走る?→最後はどこにする?→経由したい場所はある?→何時ごろ戻る?→AIに追加で伝えたいこと→条件確認→AI演出→3ルート比較、という1ステップずつ進む体験に作り替える依頼があった。既存のルート比較・スポット探索・ドライブ記録・日記・共有機能は壊さないこと、新規パッケージ(帰着日時のネイティブPicker・永続化とも)は無断で追加しないことが条件だった
- 決定:
  - **画面構成**: 新しいルート(ファイル)は増やさず、`app/conditions.tsx`を1ファイル内の`useState<StepId>`によるステップ・マシンとして全面書き換えた。`app/departure.tsx`が既に採用している「1ファイル内で`mode`を切り替える」パターンを踏襲している。`app/departure.tsx`自体は内部ロジックを一切変更せず、進捗表示(`WizardProgressHeader`、1/7)だけを追加した。`app/_layout.tsx`では`conditions`ルートのみネイティブヘッダーを消し(`headerShown: false`)、`departure`はネイティブの戻る導線を残すためそのままにした
  - **地図ピッカーの共通化**: `departure.tsx`に直書きされていた中心ピン方式のピッカー(`getCenter()`+セッションガード)と同じ考え方を、新規コンポーネント`components/location/map-point-picker.tsx`として独立実装した(departure.tsxからの抽出ではなく複製)。「最後はどこにする?→別の場所」「経由したい場所→地図から選ぶ」の両方で再利用している
  - **データ構造**: `DriveConditions`に`finalDestination`(別の場所の実座標)・`viaPoints`(ユーザーが確定的に立ち寄りたい場所の配列)・`aiNote`(自由記述)を追加。`returnDeadline`は`"HH:mm"`からISO 8601形式(例 "2026-09-03T02:00:00+09:00")に変更した。`Mood`は`short`(短時間)・`homeFocused`(帰宅時間重視)を`driveFocused`(とにかく走りたい)・`omakase`(おまかせ)に差し替え、`AvailableTime`に`day`(1日)・`unspecified`(指定なし)を追加した。`DriveConditions`のフィールドへ実際に触れているのは`app/conditions.tsx`(生成)・`types/drive.ts`(`summarizeDriveConditions`等)・`services/route/time-budget.ts`(予算計算)・`services/route/mock-route-provider.ts`(`moods`/`detourLevel`/`returnTarget`/`viaPoints`/`finalDestination`参照)の4箇所だけであることを事前に調査済みで、他の画面(`route-compare.tsx`・`route-summary.tsx`・`spot-discovery.tsx`・`route-plan.tsx`・`spot-route-plan.ts`)は不透明なオブジェクトとして受け渡すか`summarizeDriveConditions()`経由でしか見ないため、フィールド追加は無改修で済んだ
  - **帰着日時のISO化**: `services/route/time-budget.ts`の`resolveNextDeadline`(HH:mm前提の翌日ロールオーバー処理)を削除し、`new Date(iso)`で直接パースするだけの単純な実装に置き換えた。ISO文字列自体が日付を含むため、日またぎの特別処理が不要になった。「帰着日時はすでに過ぎている」という判定も、今回は実際の日付を含む値のため意味のあるチェックとして復活させている(前回のHH:mmのみの実装では日付があいまいで誤判定の原因だったため一時的に削除していたもの)
  - **ISO文字列の生成**: 新規パッケージなしで実装するため、`toIsoWithOffset(date)`という自前のフォーマッタを`app/conditions.tsx`に実装し、`date.getTimezoneOffset()`からローカルのタイムゾーンオフセット付きISO文字列(例 "+09:00")を組み立てている。日時の入力自体も、過去に実機で動作しなかった`@react-native-community/datetimepicker`は使わず、既存の数字入力欄(`TextInput`)パターンと日付チップ(今日/明日/明後日/日付を指定)の組み合わせで実装した
  - **15分未満のブロック**: 「自分で入力」の合計時間が15分未満の場合、警告を出すだけでなく「次へ」ボタンを無効化して進めないようにした(クランプで丸めて黙って通す従来の挙動とは異なる、明示的な要件)
  - **経由したい場所**: このフェーズではルート形状(ポリライン)には反映せず、ルート名・おすすめ理由の差別化材料としてのみ使う(`mock-route-provider.ts`の`highlight`文言に反映)。実際に経路をその地点経由に変形する処理は、モックのルート生成ロジック(二分探索によるスケール決定)への影響が大きく、プロトタイプの規模に見合わないため対象外とした
  - **保存済み場所・最近使った場所**: `services/location/home-location-store.ts`と同じパターン(expo-secure-store、モジュールキー+get/save/clear、JSON検証)で`saved-places-store.ts`・`recent-places-store.ts`を新設した。専用の管理(一覧・編集・削除)画面は作らず、「経由したい場所」ステップで地図から場所を確定した直後に、任意で名前を付けて保存できる軽量なUIとした
  - **「あなたに合うドライブを考えています」演出**: 本物のAI APIにはまだ接続せず、`THINKING_MESSAGES`を順番に表示するだけの固定時間モック待機とした。`handleSubmit`内の該当箇所だけを実際のAI呼び出しに差し替えられるよう、待機処理とルート取得処理を1つの非同期関数にまとめている
  - **条件確認からの再編集**: 各項目の「変更」は、ステップ・マシンの`step`をジャンプ先へ変更するだけの内部状態操作で実装した(ページ遷移なしで入力済み内容がそのまま残る)。ルート比較画面の「条件を変える」ボタンは`/conditions`へ`push`するだけで、ウィザード側は`useDriveFlow()`の`conditions`が既に存在する場合、自動的に条件確認ステップから始まるようにしている
- 理由: 単一ファイル内のステップ・マシンは、この設計判断のとおり`app/departure.tsx`で既に実績のあるパターンであり、expo-routerでステップごとに新規ファイル・新規ルートを作る場合に比べて、戻る挙動・入力保持・部分編集をローカルstateの操作だけで実現でき、プロトタイプの規模に対してリスクが小さい。地図ピッカーの共通化も、`departure.tsx`自体を変更するリスクを避けつつ重複コードを減らす現実的な折衷案である
- 代替案: ウィザードの各ステップをexpo-routerの別ファイル(別ルート)にする案も検討したが、7ステップ分のガード処理・戻る挙動・`app/_layout.tsx`登録が増え、実装量とバグの入り込む余地が大きく増えるため見送った。帰着日時にネイティブの日時Pickerパッケージを再導入する案も検討したが、過去に`@react-native-community/datetimepicker`が実機で動作しなかった経緯(本ファイルの2026-09-01のエントリ参照)があり、今回は新規パッケージなしで実装可能だったため見送った。「経由したい場所」を実際のルート形状に反映する案(ジオメトリの再計算)も検討したが、大規模な変更になるため今回は表示上の差別化のみに留めた
- 代替案: 条件入力画面側だけ日付またぎに対応した判定へ修正する案もあったが、`resolveEffectiveTimeBudget`側の同日限定ロジックが残ったままだと、画面表示は正しくても実際のルート生成時に矛盾したエラーが再発しかねないため採用しなかった。日付をまたぐ判定は今回「翌日まで」に限定し、2日以上先を指定できるようなUI(日付選択)は要件外のため追加していない
