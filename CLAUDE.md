@AGENTS.md

# Claude Code 向け追加ルール

上記の [AGENTS.md](./AGENTS.md) の共通ルールに加えて、Claude Codeは以下を守る。

- **大きな変更は最初に計画を提示する。** 新機能追加、複数ファイルにまたがる変更、依存関係の追加、設計に関わる変更を行う前に、変更内容・方針を整理して提示し、進め方に合意を得てから実装に着手する
- **不要なパッケージを勝手に追加しない。** ユーザーから明示的に依頼されていない、または計画で合意していないパッケージ・ライブラリを追加しない
- **実装だけで終わらせない。** コードの変更後は、型チェック(`npx tsc --noEmit`)・lint(`npm run lint`)・起動確認(`npx expo start`)による検証を行い、その結果を報告する。また README.md / docs/PROGRESS.md / CHANGELOG.md など関連ドキュメントの更新まで行って初めて完了とする
