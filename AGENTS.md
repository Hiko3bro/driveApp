# AI開発者向け共通ルール

このファイルは、Claude Code・Codexを含む、このリポジトリで作業するすべてのAIエージェント(および人間の開発者)が守るべき共通ルールです。

## Expoについて

Expoの仕様は頻繁に変わります。実装前に必ず該当バージョンの公式ドキュメントを確認してください。

- 現在のSDKバージョン: v54
- 参照先: https://docs.expo.dev/versions/v54.0.0/

## ブランチ・変更管理

- **mainブランチへ直接変更・コミット・pushしない。** 作業は必ず feature/fix/setup などの作業ブランチで行い、Pull Request経由でmainへ反映する
- 大きな変更(複数ファイルにまたがる変更、依存関係の追加、設計に関わる変更)は、着手前に変更内容を整理し、可能であればユーザーの承認を得てから進める

## 秘密情報・個人情報の取り扱い

- APIキー、トークン、パスワード、認証情報などをコード・設定ファイル・コミットメッセージに含めない
- 環境変数が必要な場合は、実際の値を `.env` に置き(`.gitignore` 対象)、`.env.example` にはキー名のみを記載する
- コミット・PR作成前に、秘密情報が含まれていないか必ず確認する
- **位置情報・自宅情報は特に慎重に扱う。** ユーザーの実際の位置情報、自宅住所、行動履歴、移動履歴などの個人情報を、サンプルコード・テストデータ・ログ・ドキュメント・コミットに含めない。動作確認用のダミーデータには実在しない/特定できない座標・住所を使用する

## 実装後に必ず行うこと

機能追加・修正を行った場合、実装だけで完了とせず、以下をすべて行ってから完了とする。

1. **検証**
   - 型チェック: `npx tsc --noEmit`
   - lint: `npm run lint`
   - 起動確認: `npx expo start` でアプリが起動し、変更箇所が意図通り動作することを確認する
2. **ドキュメント更新**
   - [README.md](./README.md): ユーザー・進捗確認アプリから見える「現在の正しい状態」を反映する
   - [docs/PROGRESS.md](./docs/PROGRESS.md): 作業日ごとの詳細な進捗(完了したこと/確認したこと/未完了/次にやること)を記録する
   - [CHANGELOG.md](./CHANGELOG.md): ユーザーから見える変更点を記録する
   - 設計上の重要な判断を行った場合は [docs/DECISIONS.md](./docs/DECISIONS.md) にも記録する

## パッケージ・依存関係

- 明示的に依頼されていないパッケージ・ライブラリを勝手に追加しない
- 依存関係を追加する場合は、必要性と代替案を検討したうえで最小限にする

## その他

- 既存のExpoテンプレート由来のファイル・設定を、必要性がないのに削除・改変しない
- 不明点や判断に迷う場合は、推測で進めず確認する
